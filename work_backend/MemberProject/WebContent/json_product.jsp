<%@ page language="java" contentType="application/json; charset=UTF-8"
    pageEncoding="UTF-8"%>
{
	"code" : "${product.code}", 
	"name" : "${product.name}",
	"price" : "${product.price}",
	"stock" : "${product.stock}",
	"memo" : "${product.memo}",
	"reg_mb_id" : "${product.reg_mb_id}",
	"reg_date" : "${product.reg_date}",
	"up_mb_id" : "${product.up_mb_id}",
	"up_date" : "${product.up_date}"
}